{
  "scripts": {
    "json:server": "json-server --watch db.json --port 5000 -m ./node_modules/json-server-auth"
  },
  "dependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.1.0"
  }
}
